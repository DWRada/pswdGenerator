# 🛡️ Password Generator v1.0 - Security Lab

Este é um projeto de um **Gerador de Senhas Seguras** desenvolvido durante a minha transição de carreira para Desenvolvimento e Segurança da Informação. O foco principal foi criar uma ferramenta que siga o princípio de **Secure by Default** (Seguro por Padrão).

## 🚀 Tecnologias Utilizadas
* **HTML5:** Estruturação semântica da interface.
* **CSS3:** Estilização com temática "Cyber Hacker" e foco em UX.
* **JavaScript (ES6+):** Lógica de programação, manipulação de DOM e Web APIs.

## 🧠 O que eu aprendi e apliquei neste projeto:

### 1. Lógica de Programação e Algoritmos
* **Controle de Fluxo:** Uso de loops (`for`) para geração dinâmica de caracteres baseado na escolha do usuário.
* **Manipulação de Strings e Arrays:** Uso dos métodos `.split()`, `.sort()` e `.join()` para transformar dados e criar aleatoriedade.
* **Matemática Computacional:** Aplicação de `Math.random()` e `Math.floor()` para sorteio de índices dentro de conjuntos de dados (charsets).

### 2. Engenharia de Segurança (Cybersecurity Mindset)
* **Garantia de Requisito:** Implementação de lógica para assegurar que toda senha gerada contenha, obrigatoriamente, números e símbolos especiais, mitigando senhas fracas.
* **Entropia e Shuffle:** Uso de um algoritmo de embaralhamento para evitar padrões previsíveis na estrutura da senha (tornando-a mais resistente a ataques de força bruta).

### 3. Web APIs e DOM (Document Object Model)
* **Manipulação de Eventos:** Conexão de funções JavaScript a eventos de clique no HTML (`onclick`).
* **Clipboard API:** Implementação da funcionalidade de cópia para a área de transferência usando `navigator.clipboard.writeText` com tratamento de promessas (`.then()`).
* **Interatividade:** Captura de dados em tempo real através de inputs do tipo `range`.

---
## 🛠️ Como rodar o projeto
1. Este projeto estará disponível no gitpages.
2. Ou pode clonar este repositorio fazer o download dos arquivos index.html - script.js - style.css
3. Após o download dos arquivos abra o arquivo index.html no seu navegador de preferido e pronto.

**Desenvolvido por Douglas William Radaelli** 🚀
